export default class ShakeSimulator {
    private isRunning;
    private intervalId?;
    constructor();
    startShakeSimulation(duration: number | undefined, callback: Function): void;
    private randomAcceleration;
    private stopShakeSimulation;
}
